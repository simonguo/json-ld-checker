# Icon sources

These SVG files are the editable sources for the extension icons. Chrome loads
the generated PNG files from `public/icons/png/`; keep the manifest paths
pointing to those runtime assets.

Source-only files live outside `public/` so they are not copied into release
packages.
